import * as React from "react";
import './Search.css';

const Search = () => {
    return (
        <form>
            <input type="text" className='Search'/>
        </form>
    );
};

export default Search;